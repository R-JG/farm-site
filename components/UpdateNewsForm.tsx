'use client';

import { useState, ChangeEvent, FormEvent } from 'react';
import { HomePost } from '@/utils/types';

type Props = {
  allPosts: HomePost[]
};

const UpdateNewsForm = ({ allPosts }: Props) => {
  return (
    <form></form>
  );
};

export default UpdateNewsForm;